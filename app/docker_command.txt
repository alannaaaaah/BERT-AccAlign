# build image
    $ docker build -t alannah/otalign .

# run image
    $ docker run -dit alannah/otalign

# run container
    $ docker exec -it alannah-training bash

# check docker status:
    $ docker ps -a

# check nvdia gpu status
    $ nvidia-smi